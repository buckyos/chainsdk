[TOC]
# 命令行格式
> create|covert --option [value] [--option [value], ...]

# create命令
> create 
返回新的密钥和对应地址

# convert命令
> convert --secret $
> convert --pubkey $
+ 选项
    + secret 输入私钥返回地址和公钥
    + pubkey 输入公钥返回地址